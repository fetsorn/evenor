import { isTwig, searchParamsToQuery } from "./pure.js";
import { newUUID } from "./record.js";
import {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
} from "./store.js";

export {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  isTwig,
  searchParamsToQuery,
  newUUID,
};

export default {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  isTwig,
  searchParamsToQuery,
  newUUID,
};
