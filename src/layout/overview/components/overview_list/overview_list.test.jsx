import { test, expect } from "vitest";
import { render, fireEvent } from "@solidjs/testing-library";
import { OverviewList } from "./overview_list.jsx";

test("overview list", async () => {
  expect(false).toBe(true);
});
