
                                {event && (
                                    <Button
                                        type="button"
                                        title={t("line.button.save")}
                                        onClick={onSave}
                                    >
                                        💾
                                    </Button>
                                )}
