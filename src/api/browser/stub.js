export const uuid = "a";

export const name = "e";

export const dir = "a-e";

export const dirpath = `/${dir}`;

export const query = { a: "b" };

export const entry = { c: "d" };

export const overview = [entry];

export const remote = "g";

export const url = "e";

export const token = "f";

export const filename = "h";

export const filepath = `${dirpath}/h`;

export const content = "i";

export default {
  uuid,
  name,
  dir,
  dirpath,
  query,
  entry,
  overview,
  remote,
  url,
  token,
  content,
  filename,
  filepath,
};
