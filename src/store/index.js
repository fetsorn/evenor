import { searchParamsToQuery } from "./pure.js";
import {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRecordCreate,
  onRepoChange,
  onSearch,
  setSpoilerOpen,
  getSpoilerOpen,
  getFilterQueries,
  getFilterOptions,
  getSortedRecords,
} from "./store.js";

export {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRecordCreate,
  onRepoChange,
  onSearch,
  searchParamsToQuery,
  setSpoilerOpen,
  getSpoilerOpen,
  getFilterQueries,
  getFilterOptions,
  getSortedRecords,
};

export default {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRecordCreate,
  onRepoChange,
  onSearch,
  searchParamsToQuery,
  setSpoilerOpen,
  getSpoilerOpen,
  getFilterQueries,
  getFilterOptions,
  getSortedRecords,
};
