# Document Title

track modification date to each record
read from git? git blame?

there is a way to judge when a given line of the csvs tablet was last modified. probably will slow down all queries, but maybe could be done in a separate thread afterwards, or on demand.
