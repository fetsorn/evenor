import { searchParamsToQuery } from "./pure.js";
import {
  StoreContext,
  store,
  onRecordEdit,
  onRecordEditPrime,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  setSpoilerOpen,
  getSpoilerOpen,
} from "./store.js";

export {
  StoreContext,
  store,
  onRecordEdit,
  onRecordEditPrime,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  searchParamsToQuery,
  setSpoilerOpen,
  getSpoilerOpen,
};

export default {
  StoreContext,
  store,
  onRecordEdit,
  onRecordEditPrime,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  searchParamsToQuery,
  setSpoilerOpen,
  getSpoilerOpen,
};
