import { newUUID } from "./record.js";
import { searchParamsToQuery } from "./pure.js";
import {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  setSpoilerOpen,
  getSpoilerOpen,
} from "./store.js";

export {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  newUUID,
  searchParamsToQuery,
  setSpoilerOpen,
  getSpoilerOpen,
};

export default {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  newUUID,
  searchParamsToQuery,
  setSpoilerOpen,
  getSpoilerOpen,
};
