import { newUUID } from "./record.js";
import { searchParamsToQuery } from "./pure.js";
import {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
} from "./store.js";

export {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  newUUID,
  searchParamsToQuery,
};

export default {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  newUUID,
  searchParamsToQuery,
};
