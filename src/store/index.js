import { searchParamsToQuery } from "./pure.js";
import {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  setSpoilerOpen,
  getSpoilerOpen,
} from "./store.js";
import {
  addLeafValue,
  onFieldRemove,
  onFieldChange,
  onFieldItemChange,
  onFieldItemRemove,
} from "./foo.js";

export {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  searchParamsToQuery,
  setSpoilerOpen,
  getSpoilerOpen,
  addLeafValue,
  onFieldRemove,
  onFieldChange,
  onFieldItemChange,
  onFieldItemRemove,
};

export default {
  StoreContext,
  store,
  onRecordEdit,
  onRecordSave,
  onRecordWipe,
  onRepoChange,
  onSearch,
  searchParamsToQuery,
  setSpoilerOpen,
  getSpoilerOpen,
  addLeafValue,
  onFieldRemove,
  onFieldChange,
  onFieldItemChange,
  onFieldItemRemove,
};
