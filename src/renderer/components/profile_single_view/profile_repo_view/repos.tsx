/* import * as React from "react";
 *
 * export default function Repos({ repoNames, repoDelete }) {
 *   return (
 *     <div>
 *       {repoNames.map((repo: any, idx: any) => (
 *         <ReposItem repo={repo} idx={idx} repoDelete={repoDelete} />
 *       ))}
 *     </div>
 *   );
 * } */
